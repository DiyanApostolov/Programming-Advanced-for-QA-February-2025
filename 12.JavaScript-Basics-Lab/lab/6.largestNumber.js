function solve(a, b, c) {
    // The Math.max function is used to find the largest number among a, b, and c
    // Template literals are used to format the output string that includes the maximum number
    console.log(`The largest number is ${Math.max(a, b, c)}.`);
  }
  

solve(1, 2, 3);
