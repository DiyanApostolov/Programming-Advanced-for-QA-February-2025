// step 1 --> npm init --> fill data default values are in (), if we want custom values we just type it
// step 2 --> observe package.json

import { sum } from './calculator.js';
import { Log } from 'console-log-alert';

Log.blue(sum(1, 5));
Log.pink(sum(1, 5));